If $A$ is a [[Sets|set]], the identity map (or [[Functions|function]]) is the mapping $I_{A}:A\to A$ taking every $x\in A$ to itself. Thus
$$
I_{A}=\left\{ (x,x)\mid x\in  A \right\}
$$
If $f$ maps $A$ to $B$, then clearly
$$
f \circ I_{A}=f=I_{B}\circ  f
$$

#Mathematics #Functions